__pycache__/
*.pyc
.venv/
.pytest_cache/
.ruff_cache/
.DS_Store
dist/

# LangGraph dev-server local runtime state
.langgraph_api/
# Generated deploy artifact (regenerate with: langgraph dockerfile Dockerfile)
Dockerfile

# Secrets
.env

# Never commit the real/private JD-gap corpus — only the *.stub.yaml is public.
# (See CLAUDE.md "Build in public": agent public, career data private.)
data/*.private.yaml
data/jd-gaps.yaml
data/jd-gaps.real.yaml
# Private, personal study lists (real daily-use inputs).
data/my-*.yaml
